{
  "name": "@catatpro/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:generate:control": "drizzle-kit generate --config drizzle.config.control.ts",
    "db:generate:org": "drizzle-kit generate --config drizzle.config.org.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.6",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "drizzle-kit": "^0.31.10",
    "vitest": "^2.1.8"
  }
}
